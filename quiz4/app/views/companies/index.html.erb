<h1>Companies</h1>
<hr>
<% @companies.each do |company| %>
  <div>
    <h2><%= link_to company.name, company_path(company) %></h2>
    <p><%= link_to "Delete", company_path(company), method: :delete %>
  </div>
  <hr>
<% end %>
